<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- The night configuration deliberately repeats the light theme: the app
         has one palette, and inheriting a dark parent here would flip the
         system bars against a surface that stays light. -->
    <style name="Theme.betteraimaira" parent="Theme.MaterialComponents.Light.NoActionBar">
        <item name="android:windowBackground">@color/app_background</item>
        <item name="android:windowLightStatusBar">true</item>
        <item name="android:forceDarkAllowed" tools:targetApi="q">false</item>
    </style>
</resources>
